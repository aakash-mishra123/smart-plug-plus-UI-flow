// theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    typography: {
        fontFamily: '"Roobert ENEL", "Roboto", "Helvetica", "Arial", sans-serif',
    },
    // You can also add custom fontWeight, fontSize presets here
});

export default theme;
